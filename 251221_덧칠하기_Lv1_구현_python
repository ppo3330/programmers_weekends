#O(n)
def solution(n, m, section):
    answer = 0
    wall = [0]*(n+1)
    for i in section:
        wall[i] = 1
    
    i=1
    while i <= n:
        if(wall[i] == 1):
            i += m-1
            answer+=1
        i+=1
        
    return answer
