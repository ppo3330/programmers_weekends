//O(book_time)
//예약시간 이후에 10분 청소시간.

#include <string>
#include <vector>

using namespace std;

int solution(vector<vector<string>> book_time) {
    int answer = 0;
    vector<int> times(25*60, 0);
    for(int i=0;i<book_time.size();i++){
        string st = book_time[i][0];
        string ed = book_time[i][1];
        int starttime = ((st[0]-'0')*10 + (st[1]-'0'))*60 + (st[3]-'0')*10 +(st[4]-'0');
        int endtime = ((ed[0]-'0')*10 +(ed[1]-'0'))*60 + (ed[3]-'0')*10 + (ed[4]-'0') +9;
        
        for(int t=starttime; t<= endtime;t++){
            times[t]++;
            answer = max(answer,times[t]);
        }
        
    }
    
    return answer;
}
